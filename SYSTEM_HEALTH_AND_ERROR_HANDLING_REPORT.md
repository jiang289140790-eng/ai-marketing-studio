# SYSTEM_HEALTH_AND_ERROR_HANDLING_REPORT

## 交付结论

本次已完成“系统状态与运行健康收口”的本地实现与数据核验。系统状态页现在围绕“运营是否还能继续”组织信息，不再把数据库表、SQL、PostgREST 原始错误、内部地址或 Provider 原始响应直接暴露在普通界面。

本次没有新增数据库表、Migration、RLS、MCP 工具或 Secrets，也没有执行真实发布、付费图片生成或线上部署。

## 时间范围

页面支持：

- 最近 24 小时
- 最近 7 天
- 最近 30 天

运行指标、异常和完成率都会随时间范围重新查询、过滤和计算。每个数字均在卡片说明中标注当前范围。

## 核心服务

统一展示以下 11 项服务：

- Supabase
- Auth
- Storage
- Edge Function
- MCP Bridge
- AI Marketing Studio MCP
- X MCP
- AutoDL
- ComfyUI
- 发布执行器
- 指标回收器

默认界面只展开异常或警告。健康服务收拢为“所有核心服务正常”或健康服务折叠列表。

健康状态来自现有安全状态接口、执行网关状态、工作流配置和近期任务记录，不读取或显示 Token、Secret。

当前 Supabase 项目中已确认以下 Edge Functions 为 ACTIVE：

- platform
- ops-execute
- ops-status
- ops-health
- ai-gateway

当前已确认图片工作流 `emma_s1_sdxl_t2i_v01` 状态为 active；近期成功工作流记录的 Provider 为 AutoDL，因此 AutoDL 与 ComfyUI 能在系统状态页中被识别。

X MCP 的最终状态仍以登录用户调用现有健康接口后的实时结果为准，不使用数据库记录伪造“已连接”状态。

## 运行指标与完成率

页面显示：

- Agent 运行
- 工作流运行
- 发布任务
- 指标回收任务
- 失败任务
- 等待过久任务

完成率公式：

```text
完成率 = 成功任务 ÷ 已结束任务
已结束任务 = 成功 + 失败 + 已取消
```

以下状态不进入失败数，也不进入完成率分母：

- 未开始
- 草稿
- 待处理
- 排队中
- 运行中
- 已排期

等待或运行超过 1 小时的任务单独计入“等待过久”，不会被误判为失败。

### “25%”问题的根因与修复

旧页面把成功任务除以包含草稿、待处理和未开始任务在内的全部记录，因此会出现“失败任务为 0，但成功率只有 25%”的矛盾。

新页面只使用已结束任务计算完成率，并直接展示计算说明。

当前 Campaign 在最近 24 小时、7 天和 30 天的真实数据一致：

- Agent 运行：0
- 工作流运行：1
- 发布任务：1
- 指标回收：0
- 工作流失败：0
- 发布失败：0
- 已结束成功任务：1
- 发布任务当前仍为草稿/待处理，不计入分母

因此当前完成率应为：

```text
1 ÷ 1 = 100%
```

不是 25%，也不会因为草稿任务而显示失败。

## 异常列表

每条异常现在展示：

- 影响对象
- 影响范围
- 用户可理解的原因
- 是否可重试
- 推荐操作
- 错误编号
- 发生时间

支持：

- 重试：先进入相关页面确认，不直接触发付费或外部执行
- 打开相关页面
- 查看脱敏技术详情
- 本次忽略

“忽略”仅影响当前页面会话，不删除数据库记录。

## 错误脱敏

普通界面不展示：

- SQL 与查询语句
- PostgREST 完整错误
- Stack Trace
- Token、Secret、Cookie、Authorization
- Signed URL 参数
- 内网地址
- Provider 原始响应
- 原始工作流 JSON

高级详情只展示脱敏后的摘要。JWT、凭据字段、内部 URL、查询与调用栈会被隐藏或替换为安全提示。

## 运行健康检查

新增“运行健康检查”按钮。该按钮只执行：

- 服务连通性检查
- 当前用户认证状态检查
- Storage / Edge / MCP Bridge 状态读取
- 已有工作流可用性读取
- 发布与指标能力的安全状态读取
- 近期任务和异常重新加载

不会：

- 执行真实发布
- 创建发布任务
- 调用付费图片或视频生成
- 修改数据库结构
- 修改权限或凭据

## 修改文件

- `src/pages/SystemOverviewPage.jsx`
- `src/services/ops-service.js`
- `src/utils/system-health-model.js`
- `src/styles.css`
- `test/system-health-model.test.mjs`

## 验证结果

- `npm test`：通过，退出码 0
- `npm run typecheck`：通过，退出码 0
- `npm run lint`：通过，退出码 0
- `npm run build`：通过，退出码 0
- 本地页面：`http://localhost:3001/ai-marketing-studio/#/health`
- 本地 HTTP 检查：200
- 当前基线 Commit：`a307f0510970706f5a0d4919a8c28b4d1659185a`

## 页面截图与实际点击验证

本轮尝试使用登录态浏览器进行页面截图和点击验收时，桌面浏览器控制运行时无法写入其本地内核资源，系统返回：

```text
failed to write kernel assets: 系统找不到指定的路径。 (os error 3)
```

因此本报告不伪造截图，也不把构建通过声明成完整页面验收。页面服务和路由已通过 HTTP 200 验证；登录态下的视觉截图、时间范围切换和“运行健康检查”按钮点击仍需在浏览器控制运行时恢复后补验。

## 回滚方式

本次没有数据库和部署变更。需要回滚时，仅还原上述页面、服务、模型、样式与测试文件即可。不要回滚工作区中其他既有功能改动。

